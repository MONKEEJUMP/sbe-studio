# SBE Studio Codex Project Law

Rollout ID: `SBE4827-20260704`
Project path: `C:\Users\DJ PAULIEWOOD\Dropbox\PC\Desktop\sbe-ai-consulting`
Project: `sbe.studio`

## Mission

SBE Studio is the public website for Space Bot Engineering. Keep the site polished, fast, truthful, and deployable. Preserve the existing design direction unless PAULIEWOOD explicitly asks for a redesign.

## Path Status

This repo is the active workspace for the current SBE Studio site thread. For new standalone projects, product forks, handoff packages, reports, or scrape/build artifacts, use the J-drive rule from global Codex law. Do not create unrelated new project homes under Desktop, Downloads, Documents, or Dropbox Desktop.

## Tech Stack

- Next.js app.
- React and TypeScript.
- Tailwind CSS.
- pnpm package manager.
- Vercel deployment target.

## Source Of Truth

- Current Codex thread instructions from PAULIEWOOD.
- This `AGENTS.md`.
- `package.json` scripts.
- Obsidian project memory under `C:\Users\DJ PAULIEWOOD\Codex_Brain_Vault\Codex Brain\Project Index`.
- Vercel/GitHub deployment state when deployment work is requested.

## Required Skills

Use the coded PAULIEWOOD global skill pack when relevant:

- `$pw7404-1001-codex-desktop-superflow` for full Codex operating flow.
- `$pw7404-1002-openai-developer-workflow` for OpenAI Developers, Codex workflow, official-doc checks, and repo rollout planning.
- `$pw7404-1003-project-setup-law` for project setup, `.codex/actions`, worktrees, and AGENTS.md changes.
- `$pw7404-1005-verification-security` for verification, security, review, and checkpoint discipline.
- `$pw7404-1006-closeout-memory` for Obsidian memory, backup, and compact handoffs.
- `$pw7404-1007-presidential-eval-pack` for SEO, code, design, content, and launch-readiness evals.

Use `openai-docs` for current OpenAI/Codex/API facts before making durable platform decisions.

## Setup Commands

```powershell
pnpm install
pnpm dev
```

## Verification Commands

Run the narrowest useful check first:

```powershell
pnpm lint
pnpm typecheck
pnpm build
```

For frontend/design work, also browser-check desktop and mobile. Verify no overlapping text, broken assets, invisible controls, hydration errors, or misleading copy.

## Deployment Rule

Do not claim the live site is updated until the Vercel deployment is complete and `https://sbe.studio/` is verified. When changing production copy, stats, SEO, or design, check the live domain after deploy.

## Content Rules

- Brand spelling for artificial intelligence in normal prose: `Ai`.
- Tagline/motto can intentionally keep lowercase `ai` where PAULIEWOOD specified it.
- Do not expose proprietary client/project details.
- Remove visible Kalshi or proprietary Cactus/BigC/Waterfall-style references unless PAULIEWOOD explicitly approves them.
- Keep claims tied to source files, live pages, or documented project evidence.

## Security Rules

- Never commit secrets, API keys, `.env` contents, private credentials, dependency folders, caches, build output, database dumps, or noisy logs.
- Do not publish private client links or proprietary internal tools without explicit approval.
- Treat screenshots and portfolio entries as public-facing disclosure.

## Git Rules

- Check `git status --short` before edits and before final closeout.
- Do not revert user work unless PAULIEWOOD explicitly asks.
- Do not use `git reset --hard` or destructive checkout commands.
- Stage exact paths only when committing.

## Worktree And Subagent Rules

Use worktrees for risky parallel code lanes once Git is clean enough. Use subagents only for bounded slices such as audits, visual QA, security review, source-map checks, or docs synthesis. Spud remains the integrator.

## `.codex/actions`

Coded action docs live under `.codex/actions`:

- `setup-sbe-4827-20260704.md`
- `verify-sbe-4828-20260704.md`
- `release-sbe-4829-20260704.md`

Keep custom action filenames coded. `AGENTS.md` is the exception because Codex requires the exact filename.

## Obsidian Closeout

After meaningful SBE Studio work, update Obsidian with goal, files changed/inspected, commands run, verification outcome, risks, and next steps. Back up the vault to `J:\Codex_Brain_Vault_Backup`.

## Closeout

End meaningful work with:

- what was done
- where we are now
- verification run or skipped
- what happens next
